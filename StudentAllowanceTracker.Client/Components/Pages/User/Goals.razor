@page "/goals"
@inherits GoalsBase

<div class="max-w-7xl mx-auto px-4 py-6">

    <!-- Header -->
    <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 mb-8">
        <div>
            <h1 class="text-3xl font-semibold text-gray-900 mb-1"> Savings Goals</h1>
            <p class="text-sm text-gray-500">Set and track your financial goals</p>
        </div>
        <MudButton Variant="Variant.Filled"
        StartIcon="@Icons.Material.Filled.Add"
        OnClick="OpenAddDialog"
        Style="background: linear-gradient(135deg, hsl(162, 86.6%, 32.2%) 0%, hsl(162, 97%, 26.1%) 100%); color: white; text-transform: none;"
        Class="px-6 py-3 rounded-lg font-medium">
            Create Goal
        </MudButton>
    </div>

    @if (isLoading)
    {
        <div class="flex flex-col items-center justify-center py-16">
            <MudProgressCircular Color="Color.Success" Indeterminate="true" />
            <p class="text-sm text-gray-500 mt-4">Loading goals...</p>
        </div>
    }
    else
    {
        <!-- Summary Stats -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-6">

            <MudPaper Style="background: linear-gradient(135deg, hsl(162, 86.6%, 32.2%) 0%, hsl(162, 97%, 26.1%) 100%); border-radius: 1rem; padding: 0.75rem;" Elevation="0">
                <MudText Style="color: rgba(255,255,255,0.9); font-size: 0.875rem; margin-bottom: 0.125rem;">Total Saved</MudText>
                <MudText Style="color: white; font-size: 1.875rem; font-weight: 700; word-break: break-word; line-height: 1;">₱@GetTotalSaved().ToString("N2")</MudText>
            </MudPaper>

            <MudPaper Style="background: white; border: 2px solid hsl(162, 41.8%, 89.2%); border-radius: 1rem; padding: 0.75rem;" Elevation="0">
                <MudText Style="color: rgb(107, 114, 128); font-size: 0.875rem; margin-bottom: 0.125rem;">Total Target</MudText>
                <MudText Style="color: rgb(17, 24, 39); font-size: 1.875rem; font-weight: 700; word-break: break-word; line-height: 1;">₱@GetTotalTarget().ToString("N2")</MudText>
            </MudPaper>

            <MudPaper Style="background: white; border: 2px solid hsl(162, 41.8%, 89.2%); border-radius: 1rem; padding: 0.75rem;" Elevation="0">
                <MudText Style="color: rgb(107, 114, 128); font-size: 0.875rem; margin-bottom: 0.125rem;">Active Goals</MudText>
                <MudText Style="color: rgb(17, 24, 39); font-size: 1.875rem; font-weight: 700; line-height: 1;">@goals.Count(g => !g.IsCompleted)</MudText>
            </MudPaper>

            <MudPaper Style="background: white; border: 2px solid hsl(162, 41.8%, 89.2%); border-radius: 1rem; padding: 0.75rem;" Elevation="0">
                <MudText Style="color: rgb(107, 114, 128); font-size: 0.875rem; margin-bottom: 0.125rem;">Completed</MudText>
                <MudText Style="color: rgb(17, 24, 39); font-size: 1.875rem; font-weight: 700; line-height: 1;">@goals.Count(g => g.IsCompleted)</MudText>
            </MudPaper>

        </div>

        <!-- Filter Tabs -->
        <div class="mb-6">
            <MudButtonGroup Variant="Variant.Outlined" Size="Size.Small">
                <MudButton OnClick="@(() => showCompleted = false)"
                Style="@GetFilterStyle(!showCompleted)"
                Class="px-4 py-2">
                    Active
                </MudButton>
                <MudButton OnClick="@(() => showCompleted = true)"
                Style="@GetFilterStyle(showCompleted)"
                Class="px-4 py-2">
                    Completed
                </MudButton>
            </MudButtonGroup>
        </div>

        @if (GetFilteredGoals().Any())
        {
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                @foreach (var goal in GetFilteredGoals())
                {
                    var progress = goal.TargetAmount > 0 ? (double)(goal.CurrentAmount / goal.TargetAmount * 100) : 0;
                    var isCompleted = goal.IsCompleted;

                    <MudPaper Class="p-6 border-2 transition-all hover:shadow-lg"
                              Style="@($"border-color: {(isCompleted ? "hsl(162, 86.6%, 32.2%)" : "hsl(162, 41.8%, 89.2%)")}; cursor: pointer; border-radius: 1rem;")"
                              Elevation="0">

                        <!-- Goal Header -->
                        <div class="flex items-start justify-between mb-6 gap-3">
                            <div class="flex-1">
                                <MudText Typo="Typo.subtitle1" Class="text-gray-900 mb-1 font-semibold">@goal.GoalName</MudText>
                                <MudText Typo="Typo.body2" Class="text-gray-500">@goal.Description</MudText>
                            </div>
                            @if (isCompleted)
                            {
                                <MudIcon Icon="@Icons.Material.Filled.CheckCircle"
                                         Size="Size.Medium"
                                         Style="color: hsl(162, 86.6%, 32.2%);" />
                            }
                        </div>

                        <!-- Progress Bar -->
                        <div class="mb-5">
                            <div class="flex items-center justify-between mb-3">
                                <MudText Typo="Typo.body2" Class="font-medium text-gray-700">Progress</MudText>
                                <MudText Typo="Typo.body2" Class="font-semibold" Style="color: hsl(162, 86.6%, 32.2%);">
                                    @progress.ToString("F1")%
                                </MudText>
                            </div>
                            <MudProgressLinear Color="Color.Success"
                                               Value="@progress"
                                               Size="Size.Medium"
                                               Class="rounded-full" />
                        </div>

                        <!-- Amount Info -->
                        <div class="flex items-center justify-between mb-5 px-2">
                            <div>
                                <MudText Typo="Typo.caption" Class="text-gray-500 mb-2">Current</MudText>
                                <MudText Typo="Typo.body1" Class="font-bold text-gray-900">₱@goal.CurrentAmount.ToString("N2")</MudText>
                            </div>
                            <div class="text-right">
                                <MudText Typo="Typo.caption" Class="text-gray-500 mb-2">Target</MudText>
                                <MudText Typo="Typo.body1" Class="font-bold text-gray-900">₱@goal.TargetAmount.ToString("N2")</MudText>
                            </div>
                        </div>

                        <!-- Suggested Savings  -->
                        <div class="mb-5 px-2" style="min-height: 56px;">
                            @if (!isCompleted)
                            {
                                var daysLeft = (goal.TargetDate - DateTime.Today).Days;
                                var suggested = GetSuggestedSavings(goal);
                                var suggestedPerWeek = suggested.perWeek;
                                var suggestedPerMonth = suggested.perMonth;

                                <MudText Typo="Typo.body2" Class="text-gray-700">
                                    Suggested savings:
                                    <span class="font-semibold" style="color: hsl(162, 86.6%, 32.2%);">
                                        ₱@suggestedPerWeek / week
                                    </span>
                                    or
                                    <span class="font-semibold" style="color: hsl(162, 86.6%, 32.2%);">
                                        ₱@suggestedPerMonth / month
                                    </span>
                                </MudText>
                            }
                        </div>

                        <!-- Deadline -->
                        <MudPaper Class="flex items-center gap-2 mb-5 p-3 rounded-lg"
                                  Style="background-color: hsl(162, 41.8%, 95%);"
                                  Elevation="0">
                            <MudIcon Icon="@Icons.Material.Filled.CalendarToday"
                                     Size="Size.Small"
                                     Style="color: hsl(162, 86.6%, 32.2%);" />
                            <MudText Typo="Typo.body2" Class="text-gray-700">
                                @{
                                    var daysLeft = (goal.TargetDate - DateTime.Today).Days;
                                }
                                @if (isCompleted)
                                {
                                    <span>Completed on @goal.TargetDate.ToString("MMM dd, yyyy")</span>
                                }
                                else if (daysLeft > 0)
                                {
                                    <span>@daysLeft days left</span>
                                }
                                else if (daysLeft == 0)
                                {
                                    <span class="font-semibold">Due today!</span>
                                }
                                else
                                {
                                    <span class="text-red-600 font-semibold">@Math.Abs(daysLeft) days overdue</span>
                                }
                            </MudText>
                        </MudPaper>

                        <!-- Actions -->
                        <div class="flex items-center gap-2">
                            @if (!isCompleted)
                            {
                                <MudButton Variant="Variant.Filled"
                                           StartIcon="@Icons.Material.Filled.Add"
                                           OnClick="@(() => AddContribution(goal))"
                                           Style="background: linear-gradient(135deg, hsl(162, 86.6%, 32.2%) 0%, hsl(162, 97%, 26.1%) 100%); color: white; text-transform: none;"
                                           Class="flex-1 py-2 rounded-lg font-medium"
                                           Size="Size.Small">
                                    Add Money
                                </MudButton>
                            }
                            else
                            {
                                <MudPaper Class="flex-1 text-center py-2 px-4 rounded-lg font-medium"
                                          Style="background-color: hsl(162, 86.6%, 95%); color: hsl(162, 86.6%, 32.2%);"
                                          Elevation="0">
                                    <MudText Typo="Typo.body2">Goal Achieved!</MudText>
                                </MudPaper>
                            }

                            <MudIconButton Icon="@Icons.Material.Filled.Edit"
                                           Size="Size.Small"
                                           Style="color: hsl(162, 86.6%, 32.2%);"
                                           OnClick="@(() => EditGoal(goal))" />
                            <MudIconButton Icon="@Icons.Material.Filled.Delete"
                                           Size="Size.Small"
                                           Style="color: hsl(0, 84.2%, 60.2%);"
                                           OnClick="@(() => DeleteGoal(goal))" />
                        </div>

                    </MudPaper>
                }
            </div>
        }
        else
        {
            <MudPaper Class="p-12 border-2 text-center"
                      Style="border-color: hsl(162, 41.8%, 89.2%); border-radius: 1rem;"
                      Elevation="0">
                <MudIcon Icon="@Icons.Material.Outlined.TrackChanges"
                         Class="text-gray-300 mb-4"
                         Style="font-size: 5rem;" />
                <MudText Typo="Typo.h5" Class="font-semibold text-gray-500 mb-2">
                    @(showCompleted ? "No completed goals yet" : "No active goals yet")
                </MudText>
                <MudText Typo="Typo.body2" Class="text-gray-400 mb-6">
                    @(showCompleted ? "Complete your first goal to see it here" : "Start saving towards your dreams")
                </MudText>
                @if (!showCompleted)
                {
                    <MudButton Variant="Variant.Filled"
                               StartIcon="@Icons.Material.Filled.Add"
                               OnClick="OpenAddDialog"
                               Style="background: linear-gradient(135deg, hsl(162, 86.6%, 32.2%) 0%, hsl(162, 97%, 26.1%) 100%); color: white; text-transform: none;"
                               Class="px-8 py-3 rounded-lg font-medium">
                        Create Your First Goal
                    </MudButton>
                }
            </MudPaper>
        }

    }

</div>
